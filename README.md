# SQL-Funnels

This project aims at calculating overall conversion rate from a single table, and analysing specific conversion rate by doing with an A/B TEST from multiple tables.

SQLITE is the database engine used for the project; the database is created and connected, as well as all queries are run in Python environment.